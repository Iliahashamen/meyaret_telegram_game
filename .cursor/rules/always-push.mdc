---
description: Always commit and push changes after making edits
alwaysApply: true
---

# Always Push Changes

After making edits to this project:

1. Run `git add` on modified files
2. Run `git commit` with a clear message describing the changes
3. Run `git push` to push to the remote repository

Do this as part of the same workflow whenever you modify project files.
